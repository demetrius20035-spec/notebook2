# ── RepairExpert AI — runtime dependencies ──────────────────────────
# Совместимо с Python 3.11, 3.12 и 3.13.
# Используются нижние границы версий, у которых есть готовые wheel-пакеты
# для всех трёх версий Python (важно для нативных пакетов: Pillow, bcrypt,
# cryptography, pydantic-core, PySide6) — это исключает сборку из исходников.
#
# Примечание: для максимально строгой фиксации версий (ТЗ §7) можно
# закрепить точные версии через `pip freeze` после установки в целевом
# окружении.

# Backend / API
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.35
python-multipart>=0.0.9

# Pydantic (2.9+ — первые wheel-пакеты pydantic-core под Python 3.13)
pydantic>=2.9,<3
pydantic-settings>=2.5,<3

# Database
SQLAlchemy>=2.0.36,<2.1
alembic>=1.13,<2
PyMySQL>=1.1.1,<2
cryptography>=43.0.1            # AES-256-GCM + PyMySQL sha256 auth (wheels для 3.13)

# Security
bcrypt>=4.2.1,<5
PyJWT>=2.8,<3

# HTTP / LLM clients
httpx>=0.27,<0.29
openai>=1.35,<2                 # used by Yandex Cloud OpenAI-compatible API
requests>=2.32.3,<3             # used by Sber GigaChat example

# Vector store / RAG
qdrant-client>=1.9,<2

# OCR / PDF ingest
pdfminer.six>=20231228
pytesseract>=0.3.10
Pillow>=11.0.0                  # 11.0 — первые wheel-пакеты под Python 3.13

# Document generation
Jinja2>=3.1.4,<4
WeasyPrint>=62.1
qrcode[pil]>=7.4.2,<8

# GUI (6.8+ — официальная поддержка Python 3.13)
PySide6>=6.8.0.2,<6.9

# Utilities
python-dotenv>=1.0.1,<2
tenacity>=8.4,<10
